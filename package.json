{
  "name": "YouTube-PC",
  "version": "1.0.0",
  "description": "YouTube for PC",
  "main": "main.js",
 "scripts": {
  "start": "electron .",
  "build": "mkdir dist && copy index.html dist && copy preload.js dist && copy styles.css dist"
},
  "build": {
    "appId": "com.myuser.ytmusic",
    "win": {
      "target": "nsis",
      "icon": "YT.ico"
    }
  },
  "dependencies": {
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "@capacitor/ios": "^8.3.0"
  }
}
